# EnScale Local Configuration Example
# Note: EnScale runs entirely locally and requires NO external API keys or cloud services.
# These optional settings are for local environment customization only.

ENSCALE_ENV=local
ENSCALE_LOG_LEVEL=INFO
ENSCALE_PORT=8501
